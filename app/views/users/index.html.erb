<% provide :title, t("pages.users.title") %>
<div class="container">
  <h4 class="text-center my-4"><%= t "pages.users.title" %></h4>
  <div class="d-flex flex-wrap">
    <div class="container"><%== pagy_bootstrap_nav @pagy %></div>
    <%= render @users %>
  </div>
</div>
